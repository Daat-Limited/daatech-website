// DAATECH public website
// This page is intentionally static. The visible buttons and menu icon
// are presentation-only and do not navigate anywhere yet.

document.addEventListener("DOMContentLoaded", () => {
  // Keep the public landing page intentionally simple and non-interactive.
});
